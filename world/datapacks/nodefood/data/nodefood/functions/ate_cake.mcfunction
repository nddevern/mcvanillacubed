#runs once per slice
# this one is the only one that still runs on a scoreboard objective
effect give @s speed 10 0 true
effect give @s haste 10 0 true
function nodefood:food_effect_regen_2_health_slow
function nodefood:clear_food_eaten_flags